//import { Component, Prop, State, Event, EventEmitter, h } from "@stencil/core";

const baseURL = "https://publicspaces.net";

export const Metadata = {
  EN: {
    title: 'PublicSpaces',
    subtitle: 'internet for the common good',
    about: <p>PublicSpaces reclaims the internet as a force for the common good and advocates a new internet that strengthens the public domain. <a href={`${baseURL}/manifesto`} target="_blank" rel="noopener noreferrer">Read our manifesto</a> to see the values we want to see at the core of our digital lives.</p>,
    more: 'more about PublicSpaces'
  }
}
